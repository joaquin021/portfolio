---
const codeExample = `public class SeniorSoftwareEngineer {
  private String name = "Joaquín Roca Soler";
  private String[] stack = {
    "Java 21", "Vert.x", "Kafka",
    "Kubernetes", "Uber Cadence"
  };

  public void provision() {
    // Building telco @ scale
  }
}`;
---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="hero-main">
        <h1 class="name">
          JOAQUÍN<br/>ROCA SOLER
        </h1>
        <div class="accent-line"></div>
        <h2 class="title">SENIOR SOFTWARE ENGINEER</h2>
        <p class="description">
          I BUILD HIGH-PERFORMANCE SYSTEMS.<br/>
          TELECOM PROVISIONING • EVENT-DRIVEN ARCHITECTURE • KUBERNETES
        </p>
        <div class="cta-buttons">
          <a href="#contact" class="btn btn-primary">CONTACT</a>
          <a href="#experience" class="btn btn-secondary">WORK</a>
        </div>
        <div class="location">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
            <circle cx="12" cy="10" r="3"></circle>
          </svg>
          <span>ALCALÁ DE HENARES, MADRID</span>
        </div>
      </div>

      <div class="hero-code">
        <div class="code-block">
          <div class="code-header">engineer.java</div>
          <pre><code>{codeExample}</code></pre>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: var(--spacing-3xl) 0;
    background: var(--color-bg);
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: var(--spacing-3xl);
    align-items: start;
  }

  .hero-main {
    border: var(--border-width-thick) solid var(--color-border);
    padding: var(--spacing-2xl);
    background: var(--color-bg);
  }

  .name {
    font-family: var(--font-sans);
    font-size: clamp(3rem, 10vw, 6rem);
    font-weight: 900;
    line-height: 0.9;
    letter-spacing: -0.03em;
    margin-bottom: var(--spacing-md);
    color: var(--color-text);
  }

  .accent-line {
    width: 200px;
    height: var(--border-width-thick);
    background: var(--color-accent);
    margin: var(--spacing-lg) 0;
  }

  .title {
    font-family: var(--font-sans);
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    font-weight: 700;
    letter-spacing: 0.15em;
    margin-bottom: var(--spacing-lg);
    color: var(--color-text);
  }

  .description {
    font-family: var(--font-mono);
    font-size: clamp(0.875rem, 2vw, 1rem);
    line-height: 1.8;
    margin-bottom: var(--spacing-xl);
    color: var(--color-text-secondary);
    font-weight: 500;
  }

  .cta-buttons {
    display: flex;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-lg);
  }

  .btn {
    padding: 1rem 2rem;
    border: var(--border-width) solid var(--color-border);
    background: var(--color-bg);
    font-family: var(--font-sans);
    font-size: 1rem;
    font-weight: 700;
    letter-spacing: 0.05em;
    cursor: pointer;
    transition: all 0.1s ease;
    text-decoration: none;
    color: var(--color-text);
    display: inline-block;
    position: relative;
  }

  .btn-primary {
    background: var(--color-accent);
    border-color: var(--color-border);
  }

  .btn:hover {
    transform: translate(4px, 4px);
    box-shadow: -4px -4px 0 var(--color-border);
  }

  .btn:active {
    transform: translate(0, 0);
    box-shadow: none;
  }

  .location {
    display: flex;
    align-items: center;
    gap: var(--spacing-xs);
    font-family: var(--font-mono);
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-text-secondary);
    letter-spacing: 0.05em;
  }

  .location svg {
    color: var(--color-text);
  }

  /* Code Block */
  .hero-code {
    position: sticky;
    top: var(--spacing-xl);
  }

  .code-block {
    border: var(--border-width) solid var(--color-border);
    background: var(--color-bg-secondary);
    overflow: hidden;
  }

  .code-header {
    background: var(--color-bg-tertiary);
    color: var(--color-accent);
    padding: var(--spacing-sm);
    font-family: var(--font-mono);
    font-weight: 700;
    font-size: 0.875rem;
    letter-spacing: 0.05em;
  }

  .code-block pre {
    padding: var(--spacing-md);
    margin: 0;
    overflow-x: auto;
  }

  .code-block code {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    line-height: 1.6;
    color: var(--color-text);
  }

  /* Responsive */
  @media (max-width: 968px) {
    .hero-content {
      grid-template-columns: 1fr;
      gap: var(--spacing-2xl);
    }

    .hero-code {
      position: static;
      order: -1;
    }

    .code-block code {
      font-size: 0.75rem;
    }
  }

  @media (max-width: 640px) {
    .hero {
      padding: var(--spacing-2xl) 0;
    }

    .hero-main {
      padding: var(--spacing-lg);
      border-width: var(--border-width);
    }

    .name {
      font-size: 2.5rem;
    }

    .title {
      font-size: 1.125rem;
    }

    .description {
      font-size: 0.875rem;
    }

    .cta-buttons {
      flex-direction: column;
    }

    .btn {
      width: 100%;
      text-align: center;
    }

    .accent-line {
      width: 120px;
      height: var(--border-width);
    }

    .code-block code {
      font-size: 0.7rem;
    }
  }
</style>
