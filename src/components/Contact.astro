---
const contactInfo = [
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
      <polyline points="22,6 12,13 2,6"></polyline>
    </svg>`,
    label: "Email",
    value: "example@example.com",
    href: "mailto:example@example.com"
  },
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
    </svg>`,
    label: "Phone",
    value: "666666666",
    href: "tel:666666666"
  },
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
      <circle cx="12" cy="10" r="3"></circle>
    </svg>`,
    label: "Location",
    value: "Alcalá de Henares, Madrid",
    href: null
  }
];
---

<section id="contact" class="section contact-section">
  <div class="container">
    <div class="contact-content">
      <div class="contact-info-section fade-in">
        <h2 class="section-title">Let's Connect</h2>
        <p class="contact-intro">
          I'm always interested in hearing about new opportunities,
          challenging projects, or just a friendly chat about technology
          and software architecture.
        </p>

        <div class="contact-cards">
          {contactInfo.map((info, index) => (
            info.href ? (
              <a
                href={info.href}
                class="contact-card"
                style={`animation-delay: ${(index + 1) * 0.1}s;`}
              >
                <div class="contact-icon" set:html={info.icon} />
                <div class="contact-details">
                  <span class="contact-label">{info.label}</span>
                  <span class="contact-value">{info.value}</span>
                </div>
              </a>
            ) : (
              <div
                class="contact-card"
                style={`animation-delay: ${(index + 1) * 0.1}s;`}
              >
                <div class="contact-icon" set:html={info.icon} />
                <div class="contact-details">
                  <span class="contact-label">{info.label}</span>
                  <span class="contact-value">{info.value}</span>
                </div>
              </div>
            )
          ))}
        </div>

        <div class="education-card fade-in" style="animation-delay: 0.4s;">
          <div class="education-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 10v6M2 10l10-5 10 5-10 5z"></path>
              <path d="M6 12v5c3 3 9 3 12 0v-5"></path>
            </svg>
          </div>
          <div class="education-content">
            <h3 class="education-degree">Telecommunications Engineering</h3>
            <p class="education-school">Universidad Politécnica de Cartagena</p>
            <p class="education-year">2009 - 2014</p>
          </div>
        </div>
      </div>

      <div class="contact-visual fade-in" style="animation-delay: 0.5s;">
        <div class="availability-card">
          <div class="availability-status">
            <span class="status-dot"></span>
            <span class="status-text">Available for opportunities</span>
          </div>
          <p class="availability-note">
            Currently working as Senior Software Engineer at Grupo MasOrange.
            Open to discussing exciting projects and collaborations.
          </p>
        </div>

        <div class="quick-facts">
          <h3 class="quick-facts-title">Quick Facts</h3>
          <div class="facts-list">
            <div class="fact-item">
              <span class="fact-number">10+</span>
              <span class="fact-label">Years Experience</span>
            </div>
            <div class="fact-item">
              <span class="fact-number">5</span>
              <span class="fact-label">Certifications</span>
            </div>
            <div class="fact-item">
              <span class="fact-number">3</span>
              <span class="fact-label">Major Projects</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <p class="footer-text">
        © 2025 Joaquín Roca. Senior Software Engineer specializing in telecom provisioning systems.
      </p>
      <p class="footer-note">
        Built with Astro
      </p>
    </div>
  </div>
</footer>

<style>
  .contact-section {
    background: var(--color-bg);
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-3xl);
    align-items: start;
  }

  .contact-info-section {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xl);
  }

  .section-title {
    margin-bottom: var(--spacing-sm);
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .contact-intro {
    color: var(--color-text-secondary);
    font-size: 1.125rem;
    line-height: 1.8;
  }

  .contact-cards {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }

  .contact-card {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    padding: var(--spacing-md);
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    transition: var(--transition);
    animation: fadeIn 0.6s ease-out forwards;
  }

  a.contact-card {
    cursor: pointer;
  }

  .contact-card:hover {
    transform: translateX(5px);
    border-color: var(--color-primary);
    background: var(--color-bg-tertiary);
  }

  .contact-icon {
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, var(--color-primary)20, var(--color-secondary)20);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-primary);
    flex-shrink: 0;
  }

  .contact-details {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .contact-label {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    font-weight: 500;
  }

  .contact-value {
    font-size: 1.125rem;
    color: var(--color-text);
    font-weight: 500;
  }

  /* Education Card */
  .education-card {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    padding: var(--spacing-md);
    display: flex;
    gap: var(--spacing-md);
  }

  .education-icon {
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, var(--color-secondary)20, var(--color-accent)20);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-secondary);
    flex-shrink: 0;
  }

  .education-content {
    flex: 1;
  }

  .education-degree {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: 0.25rem;
  }

  .education-school {
    color: var(--color-text-secondary);
    font-size: 0.95rem;
    margin-bottom: 0.25rem;
  }

  .education-year {
    color: var(--color-text-muted);
    font-size: 0.875rem;
  }

  /* Contact Visual */
  .contact-visual {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }

  .availability-card {
    background: linear-gradient(135deg, var(--color-primary)15, var(--color-secondary)15);
    border: 1px solid var(--color-primary);
    border-radius: var(--border-radius);
    padding: var(--spacing-lg);
  }

  .availability-status {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    margin-bottom: var(--spacing-md);
    font-weight: 600;
    font-size: 1.125rem;
    color: var(--color-text);
  }

  .status-dot {
    width: 12px;
    height: 12px;
    background: var(--color-primary);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  .availability-note {
    color: var(--color-text-secondary);
    line-height: 1.6;
  }

  .quick-facts {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    padding: var(--spacing-lg);
  }

  .quick-facts-title {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: var(--spacing-md);
  }

  .facts-list {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--spacing-md);
  }

  .fact-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: var(--spacing-md);
    background: var(--color-bg-tertiary);
    border-radius: 8px;
  }

  .fact-number {
    font-size: 2rem;
    font-weight: 700;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.25rem;
  }

  .fact-label {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
  }

  /* Footer */
  .footer {
    background: var(--color-bg-secondary);
    border-top: 1px solid var(--color-border);
    padding: var(--spacing-xl) 0;
    margin-top: var(--spacing-3xl);
  }

  .footer-content {
    text-align: center;
  }

  .footer-text {
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-xs);
  }

  .footer-note {
    color: var(--color-text-muted);
    font-size: 0.875rem;
  }

  /* Responsive */
  @media (max-width: 968px) {
    .contact-content {
      grid-template-columns: 1fr;
      gap: var(--spacing-2xl);
    }

    .facts-list {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .contact-card {
      flex-direction: column;
      text-align: center;
    }

    .education-card {
      flex-direction: column;
      text-align: center;
    }
  }
</style>
